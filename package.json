{
  "name": "norbertbarna-portfolio",
  "version": "1.0.0",
  "description": "Norbert Barna - AI-Driven Product Design Lead - Portfolio",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node scripts/check-site.mjs && node scripts/check-responsive.mjs && node scripts/check-motion.mjs && node scripts/check-server.mjs && node scripts/check-editorial-media.mjs && node scripts/check-design.mjs",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.22.2"
  },
  "engines": {
    "node": "22.x"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.62.1"
  }
}
